name: Frontend Auto Deploy

on:
  push:
    branches: [ "main" ] # main 브랜치에 코드가 올라오면 실행

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. 내 코드 내려받기
      - name: Checkout Code
        uses: actions/checkout@v3

      # 2. Docker Hub 로그인 (Secrets 사용)
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 3. 이미지 빌드 및 업로드 (Build & Push)
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./Dockerfile
          push: true
          # ★ 아래 [본인아이디]/[레포이름] 부분을 docker-compose.yml과 똑같이 맞춰주세요!
          tags: ${{ secrets.DOCKER_USERNAME }}/todac-frontend:latest

      # 4. 설정 파일 배달하기 (SCP) ★ 우리가 선택한 방법 B의 핵심!
      # 내 프로젝트에 있는 docker-compose.yml을 서버로 전송합니다.
      - name: Copy docker-compose to Server
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          source: "docker-compose.yml"
          target: "~/project" # 서버의 이 폴더로 배달됨

      # 5. 서버 접속해서 실행하기 (SSH)
      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          script: |
            echo "🚧 배포 시작..."
            cd ~/project
            
            # 최신 이미지 다운로드
            docker pull ${{ secrets.DOCKER_USERNAME }}/todac-frontend:latest
            
            # 컨테이너 재실행 (이미지와 설정파일 모두 최신 상태!)
            docker compose up -d --build --remove-orphans
            
            # 청소 (안 쓰는 옛날 이미지 삭제)
            docker image prune -f
            echo "✅ 배포 완료!"